#!/usr/bin/env bash

while IFS= read -r -d '' file; do
  t=$(git log -1 --format=%ci "$file" | sed 's/[-+][0-9]*$//' | sed 's/[-: ]//g' | sed 's/\(..\)$/.\1/')
  echo "$(printf "%-80s" "$file")"$'\t'"$t"
  touch -at "$t" -mt "$t" "$file"
done < <(git ls-files -z)
